import type { ColorConfig } from '../types'

/**
 * Default color theme for the radio group
 */
export const DEFAULT_COLORS: ColorConfig = {
  primary: '#f97316',
  primaryLight: '#fb923c',
  background: '#ffffff',
  borderColor: '#d1d5db',
  titleColor: '#1f2937',
  descriptionColor: '#6b7280',
  disabledColor: '#9ca3af',
  hoverColor: '#fee2e2'
}
